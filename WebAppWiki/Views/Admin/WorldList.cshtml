@model IEnumerable<WebAppWiki.Domains.World>

@{
    ViewData["Title"] = "Управление мирами";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>@ViewBag.Title</h1>
        <a asp-action="CreateWorldView" class="btn btn-primary">Создать новый мир</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr class="text-light">
                    <th>ID</th>
                    <th>Название</th>                
                </tr>
            </thead>
            <tbody>
                @foreach (var World in Model)
                {
                    <tr class="text-light">
                        <td>@World.WorldId</td>
                        <td>@World.Location</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="EditWorldView" asp-route-id="@World.WorldId" class="btn btn-sm btn-outline-warning">Редактировать</a>
                                <form asp-action="DeleteWorld" asp-route-id="@World.WorldId" method="post" style="display: inline;">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Вы уверены, что хотите удалить @World.Location?')">Удалить</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="form-group mb-3">
        <a asp-action="ManageContentView" class="btn btn-outline-secondary">Назад</a>
    </div>
</div>

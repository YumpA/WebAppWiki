@model IEnumerable<WebAppWiki.Domains.Fractions>

@{
    ViewData["Title"] = "Управление фракциями";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>@ViewBag.Title</h1>
        <a asp-action="CreateFractionView" class="btn btn-primary">Создать новую фракцию</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr class="text-light">
                    <th>ID</th>
                    <th>Название</th>                
                </tr>
            </thead>
            <tbody>
                @foreach (var Fractions in Model)
                {
                    <tr class="text-light">
                        <td>@Fractions.FractionsId</td>
                        <td>@Fractions.Name</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="EditFractionsView" asp-route-id="@Fractions.FractionsId" class="btn btn-sm btn-outline-warning">Редактировать</a>
                                <form asp-action="DeleteFractions" asp-route-id="@Fractions.FractionsId" method="post" style="display: inline;">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Вы уверены, что хотите удалить @Fractions.Name?')">Удалить</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="form-group mb-3">
        <a asp-action="ManageContentView" class="btn btn-outline-secondary">Назад</a>
    </div>
</div>

@model IEnumerable<WebAppWiki.Domains.Genre>

@{
    ViewData["Title"] = "Управление жанрами";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>@ViewBag.Title</h1>
        <a asp-action="CreateGenreView" class="btn btn-primary">Создать новый жанр</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr class="text-light">
                    <th>ID</th>
                    <th>Название</th>                
                </tr>
            </thead>
            <tbody>
                @foreach (var genre in Model)
                {
                    <tr class="text-light">
                        <td>@genre.GenreId</td>
                        <td>@genre.Name</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="EditGenreView" asp-route-id="@genre.GenreId" class="btn btn-sm btn-outline-warning">Редактировать</a>
                                <form asp-action="DeleteGenre" asp-route-id="@genre.GenreId" method="post" style="display: inline;">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Вы уверены, что хотите удалить @genre.Name?')">Удалить</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
        <div class="form-group mb-3">
        <a asp-action="ManageContentView" class="btn btn-outline-secondary">Назад</a>
        </div>
</div>
